import { StockDetailsDirective } from './stock-details.directive';

describe('StockDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new StockDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
