import { CodcodaPageDirective } from './codcoda-page.directive';

describe('CodcodaPageDirective', () => {
  it('should create an instance', () => {
    const directive = new CodcodaPageDirective();
    expect(directive).toBeTruthy();
  });
});
