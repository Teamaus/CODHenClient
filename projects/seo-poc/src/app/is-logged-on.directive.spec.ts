import { IsLoggedOnDirective } from './is-logged-on.directive';

describe('IsLoggedOnDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLoggedOnDirective();
    expect(directive).toBeTruthy();
  });
});
