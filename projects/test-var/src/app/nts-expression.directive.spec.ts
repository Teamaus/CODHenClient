import { NtsExpressionDirective } from './nts-expression.directive';

describe('NtsExpressionDirective', () => {
  it('should create an instance', () => {
    const directive = new NtsExpressionDirective();
    expect(directive).toBeTruthy();
  });
});
