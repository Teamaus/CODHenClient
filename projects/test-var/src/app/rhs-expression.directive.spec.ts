import { RhsExpressionDirective } from './rhs-expression.directive';

describe('RhsExpressionDirective', () => {
  it('should create an instance', () => {
    const directive = new RhsExpressionDirective();
    expect(directive).toBeTruthy();
  });
});
