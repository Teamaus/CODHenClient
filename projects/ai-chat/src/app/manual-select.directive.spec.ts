import { ManualSelectDirective } from './manual-select.directive';

describe('ManualSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ManualSelectDirective();
    expect(directive).toBeTruthy();
  });
});
