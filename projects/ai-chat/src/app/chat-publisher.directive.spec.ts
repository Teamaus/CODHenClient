import { ChatPublisherDirective } from './chat-publisher.directive';

describe('ChatPublisherDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatPublisherDirective();
    expect(directive).toBeTruthy();
  });
});
