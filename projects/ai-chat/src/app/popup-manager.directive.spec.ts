import { PopupManagerDirective } from './popup-manager.directive';

describe('PopupManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupManagerDirective();
    expect(directive).toBeTruthy();
  });
});
