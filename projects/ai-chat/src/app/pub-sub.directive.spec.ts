import { PubSubDirective } from './pub-sub.directive';

describe('PubSubDirective', () => {
  it('should create an instance', () => {
    const directive = new PubSubDirective();
    expect(directive).toBeTruthy();
  });
});
