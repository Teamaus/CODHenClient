import { AskDirective } from './ask.directive';

describe('AskDirective', () => {
  it('should create an instance', () => {
    const directive = new AskDirective();
    expect(directive).toBeTruthy();
  });
});
