import { ChatServiceDirective } from './chat-service.directive';

describe('ChatServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatServiceDirective();
    expect(directive).toBeTruthy();
  });
});
