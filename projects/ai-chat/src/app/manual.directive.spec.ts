import { ManualDirective } from './manual.directive';

describe('ManualDirective', () => {
  it('should create an instance', () => {
    const directive = new ManualDirective();
    expect(directive).toBeTruthy();
  });
});
