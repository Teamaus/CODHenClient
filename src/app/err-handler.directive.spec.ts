import { ErrHandlerDirective } from './err-handler.directive';

describe('ErrHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
