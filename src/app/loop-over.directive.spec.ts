import { LoopOverDirective } from './loop-over.directive';

describe('LoopOverDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopOverDirective();
    expect(directive).toBeTruthy();
  });
});
